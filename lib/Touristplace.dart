import 'package:flutter/material.dart';


class TouristPlacePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tourist Places in Bengkulu'),
      ),
      body: Center(
        child: Text('This is the tourist places page.'),
      ),
    );
  }
}